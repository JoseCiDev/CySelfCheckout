/// <reference types="cypress" />

import {
    Given, When, Then,
    elements as el,
    DataParameters,
    dataParameters
} from '../import'

describe('Teste de acesso', () => {
    before(() => { });

    beforeEach(() => {

    });

    const index = 0;
    it(`Acesso Selfcheckout `, () => {
        cy.log('teste')

    });
});
